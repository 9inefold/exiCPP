# Conformance testing

This folder contains tests based on the W3C conformance suite.

Folders are ordered like `ENCODER-TYPE/(exi|xml).(in|out)/*`.
`*.in` folders contain inputs for exicpp, `*.out` are for data generated by it.

Tests are roundtripped and run against exificient, if java is supported.
