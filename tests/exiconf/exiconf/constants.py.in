from pathlib import Path

__all__ = [
  '__author__',
  '__version__',
  'EXI_BASE_DIR',
  'TEST_SRC_DIR',
  'TEST_OUT_DIR',
  'TEST_OUT_DIR2',
  'EXI_BIN_DIR',
]

__author__ = "Ninefold"
#__email__ = "..."
__versioninfo__ = (0, 9, 1)
__version__ = ".".join(str(v) for v in __versioninfo__) + "dev"

EXI_VERSION = "@EXI_VERSION@"
BUILD_MODE  = "@CMAKE_BUILD_TYPE@"

EXI_BASE_DIR = Path("@EXI_BASE_DIR@")
TEST_SRC_DIR = Path("@TEST_SRC_DIR@")
TEST_OUT_DIR = Path("@TEST_OUT_DIR@")

TEST_OUT_DIR2 = EXI_BASE_DIR / 'tests/o2'
if not TEST_OUT_DIR2.exists():
  TEST_OUT_DIR2.mkdir(parents=True)

EXI_BIN_DIR  = Path("@CMAKE_LIBRARY_OUTPUT_DIRECTORY@")
EXI_EXECUTABLE  = EXI_BIN_DIR / 'exi-test-driver.exe'
XML_EXECUTABLE  = EXI_BIN_DIR / 'xml-test-driver.exe'

EXICPP_EXECUTABLE  = EXI_BIN_DIR / 'dual-test-driver.exe'

JAVA_EXECUTABLE = Path("@Java_JAVA_EXECUTABLE@")
EXIFICIENT_JAR  = Path("@EXIFICIENT_JAR@")
