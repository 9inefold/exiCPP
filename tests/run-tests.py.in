#!@Python3_EXECUTABLE@
# -*- coding: utf-8 -*-

import sys
from pathlib import Path

# Make sure we can find exiconf
source_root = Path("@EXI_BASE_DIR@")
sys.path.insert(0, str(source_root / 'tests' / 'exiconf'))

if __name__ == '__main__':
  from exiconf.main import main
  main()
